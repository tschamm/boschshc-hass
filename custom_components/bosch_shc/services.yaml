smokedetector_check:
  fields:
    entity_id:
      example: "binary_sensor.smokedetector_living_room"
      required: true
      selector:
        text:

trigger_scenario:
  fields:
    name:
      example: "Scenario Name"
      required: true
      selector:
        text:

trigger_rawscan:
  fields:
    command:
      example: "devices"
      required: true
      selector:
        text:
    device_id:
      example: "hdm:ZigBee:000d6"
      selector:
        text:
    service_id:
      example: "BatteryLevel"
      selector:
        text:


smokedetector_alarmstate:
  fields:
    entity_id:
      example: "binary_sensor.smokedetector_living_room"
      required: true
      selector:
        text:
    command:
      example: "CUSTOM_COMMAND"
      required: true
      selector:
        text:
