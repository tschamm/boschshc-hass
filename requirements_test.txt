# Test + runtime deps for the bosch_shc integration test suite.
# pytest-homeassistant-custom-component pulls in homeassistant + pytest fixtures.
pytest
pytest-timeout
pytest-homeassistant-custom-component
# Runtime dependency of the integration (manifest.json requirements).
boschshcpy
